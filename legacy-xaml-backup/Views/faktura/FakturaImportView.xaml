<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ASMED.WPF.ViewModels.Faktura"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="ASMED.WPF.Views.FakturaImportView"
             Height="Auto" Width="Auto" Background="#FFF7F9FA">

    <UserControl.DataContext>
        <vm:FakturaImportViewModel />
    </UserControl.DataContext>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- wiersz 0: nagłówek jak w FakturaView -->
            <RowDefinition Height="*"/>
            <!-- wiersz 1: zawartość (dwie kolumny) -->
            <RowDefinition Height="Auto"/>
            <!-- wiersz 2: przyciski -->
        </Grid.RowDefinitions>

        <!-- Wiersz 0: nagłówek -->
        <Border Grid.Row="0" Padding="8" Background="#E9ECEF" CornerRadius="4">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Import faktur" FontSize="18" FontWeight="Bold" Foreground="#1976D2"/>
                <TextBlock Text="  " />
                <TextBlock Text="(Import z katalogu A:\Import\FK)" FontSize="12" VerticalAlignment="Center" Foreground="Gray"/>
            </StackPanel>
        </Border>

        <!-- Wiersz 1: dwie kolumny -->
        <Grid Grid.Row="1" Margin="0,8,0,8" HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="232"/>
                <!-- kolumna lewa: lista plików -->
                <ColumnDefinition Width="10"/>
                <ColumnDefinition/>
                <!-- kolumna prawa: lista faktur -->
            </Grid.ColumnDefinitions>

            <!-- Kolumna lewa -->
            <DockPanel HorizontalAlignment="Left" Width="220">
                <StackPanel Width="210" HorizontalAlignment="Left" Margin="10,0,0,0">
                    <TextBlock Text="Pliki w katalogu" FontWeight="Bold"/>
                    <ListBox x:Name="FilesList" ItemsSource="{Binding Files}" SelectedItem="{Binding SelectedFile, Mode=TwoWay}" Height="340" Width="197" d:ItemsSource="{d:SampleData ItemCount=5}" HorizontalAlignment="Left" />
                </StackPanel>
            </DockPanel>

            <!-- Separator -->
            <GridSplitter Grid.Column="1" HorizontalAlignment="Center" ShowsPreview="True" Width="10"/>

            <!-- Kolumna prawa: lista faktur (virtualizacja) -->
            <Grid Grid.Column="2" Cursor="">
                <DataGrid ItemsSource="{Binding Invoices}" AutoGenerateColumns="False" IsReadOnly="False"
                          EnableRowVirtualization="True" VirtualizingPanel.VirtualizationMode="Recycling"
                          CanUserAddRows="False" Margin="4" SelectionMode="Extended"
                          FontFamily="Segoe UI" FontSize="14" HorizontalAlignment="Left">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Import" Binding="{Binding IsSelected, Mode=TwoWay}" Width="60"/>
                        <DataGridTextColumn Header="LP" Binding="{Binding Lp}" Width="50" IsReadOnly="True"/>
                        <DataGridTextColumn Header="N. Fakt." Binding="{Binding Number}" Width="160" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Data" Binding="{Binding Date, StringFormat=\{0:dd-MM-yyyy\}}" Width="110" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Wartość" Binding="{Binding Value, StringFormat=\{0:N2\}}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Firma" Binding="{Binding Company}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>

        <!-- Wiersz 2: przyciski -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="4" VerticalAlignment="Center" >
            <Button Content="Zaznacz wszystkie" Width="140" Command="{Binding SelectAllCommand}" Margin="4"/>
            <Button Content="Import - wybrane" Width="140" Command="{Binding ImportSelectedCommand}" Margin="4"/>
            <Button Content="Anuluj" Width="100" Command="{Binding CancelCommand}" Margin="4"/>
        </StackPanel>
    </Grid>
</UserControl>